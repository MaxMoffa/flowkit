/** Shared attach-file icon — replaces the 📎 emoji on the file step's "add file"
 *  button (distinct from the per-file-type icons in `file-type-icons.tsx`). */
export function AttachIcon() {
  return (
    <svg
      width="18"
      height="18"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.8"
      strokeLinecap="round"
      strokeLinejoin="round"
      aria-hidden="true"
    >
      <path d="M8.2 15.8V9.4a3 3 0 0 1 6 0v8.4a1.6 1.6 0 0 1-3.2 0V10.6" />
    </svg>
  )
}
